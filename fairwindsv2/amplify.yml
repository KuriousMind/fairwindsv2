version: 1
frontend:
    phases:
        build:
            commands:
                - 'next build'
    artifacts:
        baseDirectory: /.next
        files:
            - '**/*'
    cache:
        paths: []
